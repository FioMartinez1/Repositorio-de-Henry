function esPar(num) {
  // Retorna true si "num" es par.
  // De lo contrario, retorna false.
  // Tu código:

// return num % 2 === 0 == SI EL RESTO DE NUM DIVIDIDO 2 ES IGUAL A 0. NUM ES PAR


}

module.exports = esPar;
