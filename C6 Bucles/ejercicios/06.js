function tieneTresDigitos(num) {
  // Si el número recibido tiene tres dígitos, retorna true.
  // Caso contrario, retorna false.
  // Tu código:

var numero = num.toString().length

return (numero === 3)

}

module.exports = tieneTresDigitos;
